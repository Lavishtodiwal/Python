a = 551
digits = [int(digit) for digit in str(a)]  # Convert each digit to an integer and store it in a list
print(digits)
sum1 = sum(digits)  # Use the sum() function to calculate the sum of the list
print(sum1)